/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["https://admin.7wondersperu.com/"], // Reemplaza con el dominio desde el cual cargar imágenes
  },
};

module.exports = nextConfig;
